version: "2"

networks:
  default:
    ipam:
      driver: default
      config:
        - subnet: 10.124.124.0/16

services:
  app:
    build: .
    ports:
      - 9000:9000
    volumes:
      - .:/app
      - /app/node_modules
      - /app/bower_components

  solr:
    ports:
      - 8983:8983
    build:
      context: ./solrconfig
    entrypoint:
      - docker-entrypoint.sh
      - solr-precreate
      - stats
      - /tmp/stats
